# Copy to .env and adjust as needed
OLLAMA_URL=http://localhost:11434
# Dedicated embedding model (must match the model used to embed stored chunks)
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_GEN_MODEL=gemma3:270m
CHROMA_HOST=localhost
CHROMA_PORT=8001
LOG_LEVEL=INFO
